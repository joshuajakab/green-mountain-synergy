import React from 'react';
import Checkout from '../../components/Checkout';

const Cart = ({}) => {
    return (
        <div className='checkout-container'>
            <Checkout />
        </div>
    );
}

export default Cart;
